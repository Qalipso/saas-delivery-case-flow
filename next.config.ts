import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Onboarding hub is shipped as a static file in /public and embedded via iframe.
  // No special config needed; kept explicit for future tuning.
};

export default nextConfig;
